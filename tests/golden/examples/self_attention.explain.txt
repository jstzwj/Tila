kernel @self_attention_kernel — audit explain v1
audit format: stable sections; raw SMT-LIB is opt-in
  consts: BD=64, BM=64, BN=64
  note: grid facts are launch-time contracts and are not registered here; obligations relying on the cdiv tactic may prove (SafeUnderContract) at launch
integer launch contracts (must pass before execution):
  integer operands must fit i32 before arithmetic
  cannot prove intermediate index '*' does not overflow i32
  cannot prove intermediate index '+' does not overflow i32
  range start/end/positive step must fit i32
parameters:
  BD : Const[int, PowerOfTwo]
  BM : Const[int, PowerOfTwo]
  BN : Const[int, PowerOfTwo]
  M : i32
  N : i32
  k : Buffer[f16, (N, BD), ReadOnly, UnknownAlignment]
  k_stride0 : i32
  k_stride1 : i32
  o : Buffer[f32, (M, BD), WriteOnly, UnknownAlignment]
  o_stride0 : i32
  o_stride1 : i32
  q : Buffer[f16, (M, BD), ReadOnly, UnknownAlignment]
  q_stride0 : i32
  q_stride1 : i32
  scale : f32 | Positive
  v : Buffer[f16, (N, BD), ReadOnly, UnknownAlignment]
  v_stride0 : i32
  v_stride1 : i32
types:
  BD : i32
  BM : i32
  BN : i32
  M : i32
  N : i32
  acc : Block[f32, (BM, BD)]
  k : Buffer[f16, (N, BD), ReadOnly, UnknownAlignment]
  k_stride0 : i32
  k_stride1 : i32
  m_ok : Mask[(BM)]
  o : Buffer[f32, (M, BD), WriteOnly, UnknownAlignment]
  o_stride0 : i32
  o_stride1 : i32
  pid : i32
  q : Buffer[f16, (M, BD), ReadOnly, UnknownAlignment]
  q_stride0 : i32
  q_stride1 : i32
  q_tile : Block[f16, (BM, BD)]
  rd : Block[i32, (BD)]
  rm : Block[i32, (BM)]
  rn : Block[i32, (BN)]
  row_max : Block[f32, (BM, 1)]
  row_sum : Block[f32, (BM, 1)]
  scale : f32
  v : Buffer[f16, (N, BD), ReadOnly, UnknownAlignment]
  v_stride0 : i32
  v_stride1 : i32
facts:
  BD ∈ [1, +inf)
  BM ∈ [1, +inf)
  BN ∈ [1, +inf)
  __lane1 ∈ [0, BM)
  __lane1@-2 ∈ [0, BM)
  __lane2 ∈ [0, BN)
  __lane3 ∈ [0, BD)
  pid0 ∈ [0, __grid0)
  BD = 64 (Const)
  BM = 64 (Const)
  BN = 64 (Const)
  nonneg symbols: BD, BM, BN, M, N, __i_n0_4, __lane1, __lane1@-2, __lane2, __lane2@-2, __lane3, __lane3@-2, pid0
obligations:
  - load[q] axis 0: 0 <= (BM * pid0) + __lane1@-2 < M
      path: true
      mask: mapped(mapped((BM * pid0) + __lane1@-2 < M))
      conclusion: ProvenSafe (ProvenSafe)
      state: ProvenSafe
      source locations: line 18, line 19
      trust sources:
        - StaticFact: structural nonnegativity (pid/lane/dim/Const terms)
        - StaticFact line 18: comparison
      proof route:
        - direct predicate `(BM * pid0) + __lane1@-2 < M` (path/mask DAG) proves the upper bound; lower bound by structural nonnegativity (pid/lane/dim/Const terms)
      counterexample:
        (none)
      unknown/pending:
        (none)
      proof cache: transparent (use --show-cache for per-call telemetry)
      SMT-LIB replay: not available
      fix:
        (none)
  - load[q] axis 1: 0 <= __lane3 < BD
      path: true
      mask: mapped(mapped((BM * pid0) + __lane1@-2 < M))
      conclusion: ProvenSafe (ProvenSafe)
      state: ProvenSafe
      source locations: line 19
      trust sources:
        - StaticFact: numeric interval
        - StaticFact: structural nonnegativity (pid/lane/dim/Const terms)
      proof route:
        - numeric interval: coord __lane3 ∈ [0, 63], extent BD ∈ [64, 64] ⇒ 63 < 64; lower bound by structural nonnegativity (pid/lane/dim/Const terms)
      counterexample:
        (none)
      unknown/pending:
        (none)
      proof cache: transparent (use --show-cache for per-call telemetry)
      SMT-LIB replay: not available
      fix:
        (none)
  - load[k] axis 0: 0 <= __i_n0_4 + __lane2 < N
      path: true
      mask: mapped((mapped(__i_n0_4 + __lane2 < N)) and (__lane3@-2 >= 0))
      conclusion: ProvenSafe (ProvenSafe)
      state: ProvenSafe
      source locations: line 28, line 34
      trust sources:
        - StaticFact: structural nonnegativity (pid/lane/dim/Const terms)
        - StaticFact line 28: comparison
      proof route:
        - direct predicate `__i_n0_4 + __lane2 < N` (path/mask DAG) proves the upper bound; lower bound by structural nonnegativity (pid/lane/dim/Const terms)
      counterexample:
        (none)
      unknown/pending:
        (none)
      proof cache: transparent (use --show-cache for per-call telemetry)
      SMT-LIB replay: not available
      fix:
        (none)
  - load[k] axis 1: 0 <= __lane3@-2 < BD
      path: true
      mask: mapped((mapped(__i_n0_4 + __lane2 < N)) and (__lane3@-2 >= 0))
      conclusion: ProvenSafe (ProvenSafe)
      state: ProvenSafe
      source locations: line 34
      trust sources:
        - StaticFact: numeric interval
        - StaticFact: structural nonnegativity (pid/lane/dim/Const terms)
      proof route:
        - numeric interval: coord __lane3@-2 ∈ [0, 63], extent BD ∈ [64, 64] ⇒ 63 < 64; lower bound by structural nonnegativity (pid/lane/dim/Const terms)
      counterexample:
        (none)
      unknown/pending:
        (none)
      proof cache: transparent (use --show-cache for per-call telemetry)
      SMT-LIB replay: not available
      fix:
        (none)
  - load[v] axis 0: 0 <= __i_n0_4 + __lane2@-2 < N
      path: true
      mask: mapped(mapped(__i_n0_4 + __lane2@-2 < N))
      conclusion: ProvenSafe (ProvenSafe)
      state: ProvenSafe
      source locations: line 28, line 48
      trust sources:
        - StaticFact: structural nonnegativity (pid/lane/dim/Const terms)
        - StaticFact line 28: comparison
      proof route:
        - direct predicate `__i_n0_4 + __lane2@-2 < N` (path/mask DAG) proves the upper bound; lower bound by structural nonnegativity (pid/lane/dim/Const terms)
      counterexample:
        (none)
      unknown/pending:
        (none)
      proof cache: transparent (use --show-cache for per-call telemetry)
      SMT-LIB replay: not available
      fix:
        (none)
  - load[v] axis 1: 0 <= __lane3 < BD
      path: true
      mask: mapped(mapped(__i_n0_4 + __lane2@-2 < N))
      conclusion: ProvenSafe (ProvenSafe)
      state: ProvenSafe
      source locations: line 48
      trust sources:
        - StaticFact: numeric interval
        - StaticFact: structural nonnegativity (pid/lane/dim/Const terms)
      proof route:
        - numeric interval: coord __lane3 ∈ [0, 63], extent BD ∈ [64, 64] ⇒ 63 < 64; lower bound by structural nonnegativity (pid/lane/dim/Const terms)
      counterexample:
        (none)
      unknown/pending:
        (none)
      proof cache: transparent (use --show-cache for per-call telemetry)
      SMT-LIB replay: not available
      fix:
        (none)
  - store[o] axis 0: 0 <= (BM * pid0) + __lane1@-2 < M
      path: true
      mask: mapped(mapped((BM * pid0) + __lane1@-2 < M))
      conclusion: ProvenSafe (ProvenSafe)
      state: ProvenSafe
      source locations: line 18, line 56
      trust sources:
        - StaticFact: structural nonnegativity (pid/lane/dim/Const terms)
        - StaticFact line 18: comparison
      proof route:
        - direct predicate `(BM * pid0) + __lane1@-2 < M` (path/mask DAG) proves the upper bound; lower bound by structural nonnegativity (pid/lane/dim/Const terms)
      counterexample:
        (none)
      unknown/pending:
        (none)
      proof cache: transparent (use --show-cache for per-call telemetry)
      SMT-LIB replay: not available
      fix:
        (none)
  - store[o] axis 1: 0 <= __lane3 < BD
      path: true
      mask: mapped(mapped((BM * pid0) + __lane1@-2 < M))
      conclusion: ProvenSafe (ProvenSafe)
      state: ProvenSafe
      source locations: line 56
      trust sources:
        - StaticFact: numeric interval
        - StaticFact: structural nonnegativity (pid/lane/dim/Const terms)
      proof route:
        - numeric interval: coord __lane3 ∈ [0, 63], extent BD ∈ [64, 64] ⇒ 63 < 64; lower bound by structural nonnegativity (pid/lane/dim/Const terms)
      counterexample:
        (none)
      unknown/pending:
        (none)
      proof cache: transparent (use --show-cache for per-call telemetry)
      SMT-LIB replay: not available
      fix:
        (none)
hints:
  multiple_of(rm_base, BM)
    dependency: StaticFact at line 14: structural pid * step; integer launch guards required
  max_contiguous(rm, BM)
    dependency: StaticFact at line 14: structural contiguous span; integer launch guards required
  max_contiguous(rn, BN)
    dependency: StaticFact at line 15: structural contiguous span; integer launch guards required
  max_contiguous(rd, BD)
    dependency: StaticFact at line 16: structural contiguous span; integer launch guards required
  max_contiguous(rv, BN)
    dependency: StaticFact at line 27: structural contiguous span; integer launch guards required
effects:
  Read[BufferRegion[0:q]]
  Read[BufferRegion[1:k]]
  Read[BufferRegion[2:v]]
  Write[BufferRegion[3:o]]
aliases:
  scope: static conservative defaults
  MayAlias[BufferRegion[0:q], BufferRegion[1:k]] (distinct external parameters are conservatively MayAlias)
  MayAlias[BufferRegion[0:q], BufferRegion[2:v]] (distinct external parameters are conservatively MayAlias)
  MayAlias[BufferRegion[0:q], BufferRegion[3:o]] (distinct external parameters are conservatively MayAlias)
  MayAlias[BufferRegion[1:k], BufferRegion[2:v]] (distinct external parameters are conservatively MayAlias)
  MayAlias[BufferRegion[1:k], BufferRegion[3:o]] (distinct external parameters are conservatively MayAlias)
  MayAlias[BufferRegion[2:v], BufferRegion[3:o]] (distinct external parameters are conservatively MayAlias)
warnings:
  (none)
notes:
  (none)
deferred constraints:
  step: loop step BN >= 1
